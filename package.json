{
  "name": "quran-reader",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare:ios": "node scripts/prepare-ios-web.mjs",
    "ios:sync": "npm run prepare:ios && npx cap sync ios",
    "ios:open": "npm run ios:sync && npx cap open ios"
  },
  "dependencies": {
    "@capacitor/cli": "^8.5.0",
    "@capacitor/core": "^8.5.0",
    "@capacitor/file-transfer": "^2.0.5",
    "@capacitor/filesystem": "^8.1.3",
    "@capacitor/ios": "^8.5.0",
    "@capacitor/preferences": "^8.0.1"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
